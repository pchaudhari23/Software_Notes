Network Errors:

- Timeout — request takes too long, no response
- CORS errors — server doesn't allow your origin
- Connection refused / DNS failure — backend down or wrong URL
- Offline — no network at all

HTTP Errors: 400, 401, 403, 404, 500, 502, 503, 504
